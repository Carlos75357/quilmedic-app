name: quilmedic
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.7.0

dependencies:
  bloc: ^9.0.0
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.0
  flutter_datalogic: ^1.0.9
  flutter_dotenv: ^5.1.0
  http: ^1.3.0
  intl: ^0.20.2
  meta: ^1.16.0
  mobile_scanner: ^6.0.7
  provider: ^6.1.2
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
